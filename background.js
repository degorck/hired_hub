/**
 * hired_hub - Background Service Worker
 * 
 * Minimal service worker for future features like:
 * - Notifications
 * - Alarms for follow-up reminders
 * - Context menu integration
 */

// Currently empty - placeholder for future features
// Chrome requires a background service worker in Manifest V3

chrome.runtime.onInstalled.addListener(() => {
  console.log('hired_hub extension installed');
});
